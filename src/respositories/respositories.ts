export {};

// import { getModelForClass, ReturnModelType } from '@typegoose/typegoose';
// import { AnyParamConstructor } from '@typegoose/typegoose/lib/types';
// import { Clip } from 'src/entities/clip.entity';
// import { Token } from 'typedi';

// type ClipRepository = ReturnModelType<AnyParamConstructor<Clip>>;

// // @Service({id: ClipRepository})
// // class Something {

// // }

// // getModelForClass(Clip);

// export const CLIP_REPOSITORY = new Token<ClipRepository>('CLIP_REPOSITORY');
